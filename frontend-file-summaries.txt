{
  "frontend/src/App.tsx": "Root application component that sets up providers (Theme, QueryClient, InternetIdentity) and renders MainApp with Toaster for notifications.",
  "frontend/src/components/MainApp.tsx": "Main application logic component that handles authentication state, actor initialization state, profile setup flow, and renders appropriate screens (Login, loading spinner, or Dashboard with Header/Footer). Now waits for the actor to be fully ready before rendering the Dashboard to prevent premature queries.",
  "frontend/src/components/Header.tsx": "Application header displaying the IASA logo with updated branding text and user profile dropdown showing squad role badge along with three scorecards and logout functionality.",
  "frontend/src/components/Footer.tsx": "Footer component displaying copyright, attribution to caffeine.ai with heart icon, and version identifier \"IASA Challenge (Alpha Sandbox) v0.3\".",
  "frontend/src/components/LoginScreen.tsx": "Landing page for unauthenticated users featuring the new IASA logo from external URL, updated title \"IASA Challenge (Alpha Sandbox) v0.3\", feature highlights with icons, and Internet Identity login button.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time users to create their profile, now includes Masters as a fourth squad role option alongside Apprentice, Journeyman, and Mentor.",
  "frontend/src/components/Dashboard.tsx": "Main dashboard with three scorecard metrics and a tabbed project list; now shows skeleton loading cards while projects are being fetched from the backend instead of rendering empty tabs immediately.",
  "frontend/src/components/ProjectsTab.tsx": "Displays filtered list of projects in a responsive grid layout with loading and empty states.",
  "frontend/src/components/ProjectCard.tsx": "Card component showing project summary with status badge, pledge progress bar, prize money, payout per HH, participant count, and view details button.",
  "frontend/src/components/CreateProjectDialog.tsx": "Dialog form for creating new projects; now imports useActor to check actor availability, disables the trigger button and all form fields while the actor is initializing, and shows a \"Connecting...\" spinner on the button until the backend is ready.",
  "frontend/src/components/ProjectDetailsDialog.tsx": "Comprehensive project details modal that now fetches `userProfile` via `useGetCallerUserProfile` and passes it down to both `TasksSection` and `PledgeSection`, fixing the missing required prop errors.",
  "frontend/src/components/PledgeSection.tsx": "Pledge overview with activation progress, remaining-budget indicator, PM-only sign-off panel, and pledge lists; fixes `__kind__` double-underscore references, makes `userProfile` prop optional, and adds an informational card for non-PM users showing pending pledge count.",
  "frontend/src/components/TasksSection.tsx": "Displays project tasks with remaining-budget banner and Other Tasks Pool card; \"Create Task\" button is now visible to all authenticated users (not just creator), and the `userProfile` prop is optional (defaults to null) so it can be used without passing it from `ProjectDetailsDialog`.",
  "frontend/src/components/TaskCard.tsx": "Task card with pledge form (all authenticated users), accept/complete actions, PM-only \"Confirm HH\" approve button, and Mentor-only \"Sign Off\" button; fixes all `__kind__` (double underscore) type references and uses proper TypeScript type narrowing for `PledgeTarget`.",
  "frontend/src/components/CreateTaskDialog.tsx": "Dialog form for creating tasks, now correctly calculates remaining budget by subtracting the Other Tasks pool HH from the total, so task budgets don't overlap with the pool allocation.",
  "frontend/src/components/CompleteProjectButton.tsx": "Button component for project creators to mark projects as completed, triggering the 7-day peer rating window.",
  "frontend/src/components/PeerRatingSection.tsx": "Peer rating interface for completed projects; imports `useGetUserProfile` from the restored hook and calls `ratePeer.mutateAsync` with `ratee: Principal` directly matching the corrected mutation signature.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; re-adds the missing `useGetUserProfile` hook, fixes `useRatePeer` to use `Principal` directly as the `ratee` parameter type (no more `never`), and removes the broken `rateePrincipal` indirection.",
  "frontend/src/index.css": "Global styles with OKLCH color system defining design tokens for light and dark modes with purple/cyan accent colors.",
  "frontend/index.html": "HTML entry point with updated page title to \"IASA Challenge (Alpha Sandbox) v0.3\" and favicon pointing to the new IASA logo URL.",
  "frontend/src/components/ShareOfPoolSection.tsx": "Payout distribution component showing each participant's earned HH share and CLP payout; imports `useGetUserProfile` from the restored hook and uses a typed `share` prop instead of `any`."
}